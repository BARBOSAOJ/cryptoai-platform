# Dependencias para fine-tuning de BT (Opción C).
# Solo necesarias para entrenar — no para ejecutar el servicio.
#
# Instalar con:
#   pip install -r requirements-train.txt
#
# GPU (CUDA 12.x): añade --index-url https://download.pytorch.org/whl/cu121
# CPU únicamente: las versiones por defecto funcionan, pero el entrenamiento será lento.

torch>=2.3.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.11.0
datasets>=3.0.0
accelerate>=0.34.0
bitsandbytes>=0.44.0    # Solo GPU CUDA; ignorar el warning si no hay CUDA
sentencepiece>=0.2.0
protobuf>=4.25.0
